@using Core.Entities
@using Core.DTOs
@model PaymentTermDTO
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            @*<div class="card-header">
                <div>
                    <span><b>Deliverable Name :</b> @ViewBag.Deliverable.DeliverableName</span>
                </div>
                <div>
                    <span><b>Deliverable Start Date :</b> @ViewBag.Deliverable.StartDate.ToString("dd/MM/yyyy")</span>
                </div>
                <div>
                    <span><b>Deliverable End Date :</b> @ViewBag.Deliverable.EndDate.ToString("dd/MM/yyyy")</span>
                </div>
            </div>*@
            <div class="card-body">
                <div class="basic-form">
                    <form action="/PaymentTerm/InsertPaymentTerm?deliverableId=@ViewBag.deliverableId" method="post">
                        <div class="form-group">
                            <label>Payment Term Title</label>
                            <input class="form-control" asp-for="PaymentTermTitle" />
                            <span class="text-danger" asp-validation-for="PaymentTermTitle"></span>
                        </div>
                        <div class="form-group">
                            <label>Payment Term Amount</label>
                            <input class="form-control" asp-for="PaymentTermAmount" />
                            <span class="text-danger" asp-validation-for="PaymentTermAmount"></span>
                            @if (ViewBag.Message != null)
                            {
                                <span class="text-danger">@ViewBag.Message</span>
                            }
                        </div>
                        <div class="form-group d-flex justify-content-center"><input type="submit" value="Create" class="btn btn-primary"> </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


