@using Core.Entities
@using Core.DTOs
@model DeliverableDTO
@{
    var ProjectPhaseId = ViewBag.ProjectPhaseId;

    string[] dateErrors = new string[3] { null, null, null };
    if (ViewBag.DateErrors != null)
    {
        dateErrors = ViewBag.DateErrors;
    }
}
@*<script>
        $(function () {
            $("#StartDate").datepicker();
            $("#EndDate").datepicker();
        });
    </script>*@

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <div>
                    <span><b>Project Name :</b> @ViewBag.ProjectPhase.Phase.PhaseName</span>
                </div>
                <div>
                    <span><b>Project Start Date :</b> @ViewBag.ProjectPhase.StartDate.ToString("dd/MM/yyyy")</span>
                </div>
                <div>
                    <span><b>Project End Date :</b> @ViewBag.ProjectPhase.EndDate.ToString("dd/MM/yyyy")</span>
                </div>
            </div>
            <div class="card-body">
                <div class="basic-form mt-3">
                    <form action="/Deliverable/InsertDeliverable?ProjectPhaseId=@ProjectPhaseId" method="post">
                        <div class="row">
                            <div class="form-group col-md-6 col-sm-12">
                                <label>Deliverable Name</label>
                                <input class="form-control" asp-for="DeliverableName" />
                                <span class="text-danger" asp-validation-for="DeliverableName"></span>
                            </div>

                            <div class="form-group col-md-6 col-sm-12">
                                <label>Deliverable Description</label>
                                <input class="form-control" asp-for="DeliverableDescription" />
                                <span class="text-danger" asp-validation-for="DeliverableDescription"></span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="form-group col-md-6 col-sm-12">
                                <label>Start Date</label>
                                <input type="date" class="form-control" asp-for="StartDate" />
                                @if (dateErrors[0] != null)
                                {
                                    <span class="text-danger">@dateErrors[0]</span>
                                }
                            </div>

                            <div class="form-group col-md-6 col-sm-12">
                                <label>End Date</label>
                                <input type="date" class="form-control" asp-for="EndDate" />
                                @if (dateErrors[1] != null)
                                {
                                    <span class="text-danger">@dateErrors[1]</span>
                                }
                                <br />
                                @if (dateErrors[2] != null)
                                {
                                    <span class="text-danger">@dateErrors[2]</span>
                                }
                            </div>
                        </div>
                        <div class="form-group d-flex justify-content-center"><input type="submit" value="Create" class="btn btn-primary"> </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>